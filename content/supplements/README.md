# Supplements Directory

This directory stores per-story supplement files.
Each file is named `<story-slug>.json` and contains an array of supplement entries.

Example: `if-pride.json` contains supplements for the Pride IF story.

If a story has no supplements, no file is needed — the app shows "coming soon" automatically.
